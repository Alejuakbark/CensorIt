LicensePlateBlurFilter="License Plate Blur"
PlateRegion="License plate region"
PlateRegionHint="Pick the profile closest to plates you expect. Non-custom regions apply typical width/height ratio limits and a suggested minimum box size to cut false positives. Edit presets in the plugin source (kPlateRegionProfiles) to match your jurisdiction."
RegionCustom="Custom (sliders only)"
RegionNorthAmerica="North America (USA / Canada style)"
RegionEurope="Europe (long rectangular)"
RegionUnitedKingdom="United Kingdom"
RegionJapanKorea="Japan / South Korea"
RegionAustralasia="Australia / New Zealand"
RegionLatinAmerica="Latin America"
RegionMiddleEastAfrica="Middle East / Africa"
RegionIndia="India"
UserRegionLabel="Your jurisdiction / notes (optional)"
UserRegionLabelHint="Free text saved with the filter (e.g. Ontario passenger, EU temporary plate). Not used for detection; helps you remember which preset you tuned."
ModelPath="ONNX model (license plate detector)"
ModelHint="Use a YOLOv8-style ONNX export trained for license plates (typically one class). Export from Ultralytics: yolo export model=best.pt format=onnx opset=12. Match your OBS libobs version when compiling this plugin."
ConfThreshold="Confidence threshold"
NmsThreshold="NMS IoU threshold"
ProcessEveryN="Run detector every N frames (blur uses last result in between)"
MaxInferWidth="Detector letterbox size (pixels)"
DelayFrames="Output delay (frames)"
DelayFramesHint="Buffers video so each output frame is older than the live input by this many frames. Use with a higher Max infer width (e.g. 1920) to buy time for heavier 1080p-style detection. First N frames pass through unfiltered while the buffer fills. Large values use more RAM."
BlurSize="Blur kernel size (odd values; larger = stronger blur)"
PaddingPercent="Expand each box before blur (percent of box size)"
MinAreaPercent="Ignore detections smaller than this percent of the frame area"
DebugShowBoxes="Draw detection boxes (debug)"
